
        var removeValue = function (array, id) {
            return _.reject(array, function (item) {
                return item === id; // or some complex logic
            });
        };

        var array = ['1', '20', '50','60', '78', '90'];
        var id = '1';
     
        res.send(removeValue(array, id));
